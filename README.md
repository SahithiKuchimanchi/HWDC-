# HWDC-
This project implements a Convolutional Neural Network (CNN) to accurately classify images of handwritten digits (0-9). Built primarily using the MNIST dataset, the model leverages deep learning to achieve high accuracy in digit recognition, a foundational task in computer vision. 
